# Flight-Deal-Alert-System
Intelligent Flight Deal Alert System  An automated tool using Python and the Amadeus API to track real-time flight prices. It compares fares against a user budget and triggers instant alerts via Twilio SMS and SMTP Email when deals are found. This solution saves time by eliminating manual checks and ensures users never miss discounted tickets.
